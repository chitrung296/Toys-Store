<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Toy Store - Buy Toys for Kids &amp, Children Games Online</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
</head>
<style>
    /* CSS code here */
    h1 {
    font-family: 'Arial', sans-serif; /* Chọn font chữ */
    font-size: 36px; /* Kích thước chữ */
    color: #333; /* Màu chữ */
    text-align: center; /* Căn giữa */
    margin: 20px 0; /* Khoảng cách trên và dưới */
    padding: 10px; /* Khoảng cách bên trong */
    background-color: #ff7272; /* Màu nền */
    border-radius: 8px; /* Bo góc */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Đổ bóng */
}

    /* Thiết lập font chữ */
    body {
        background-color: #c87b7b;
        font-family: Arial, sans-serif;
    }

    /* Tạo đường viền cho bảng */
    table {
        border-collapse: collapse;
        width: 100%;
    }

    /* Tạo đường viền cho các ô trong bảng */
    td,
    th {
        border: 1px solid #dddddd;
        padding: 8px;
    }

    /* Tạo nền màu cho các hàng xen kẽ trong bảng */
    tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    /* Thiết lập màu nền và màu chữ cho tiêu đề bảng */
    th {
        background-color: #c21717;
        color: white;
    }

    /* Tạo hiệu ứng hover khi di chuột vào dòng dữ liệu */
    tr:hover {
        background-color: #f5f5f5;
    }

    /* Truy cập vào navbar */
    .navbar {
        background-color: #ffffff;
        /* Màu nền */
        box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
        /* Hiệu ứng bóng đổ */
    }

    /* Định dạng logo hoặc tiêu đề */
    .navbar-brand {
        color: #333333;
        /* Màu chữ */
        font-weight: bold;
        /* Đậm */
    }

    /* Định dạng các link trong navbar */
    .navbar-nav .nav-link {
        color: #333333;
        /* Màu chữ */
    }

    /* Định dạng nút toggle cho màn hình nhỏ */
    .navbar-toggler-icon {
        color: #333333;
        /* Màu biểu tượng */
    }

    /* Định dạng khi hover (di chuột qua) các link */
    .navbar-nav .nav-link:hover {
        color: #007bff;
        /* Màu chữ khi hover */
    }

    /* Định dạng menu dropdown */
    .dropdown-menu {
        background-color: #ffffff;
        /* Màu nền */
    }

    /* Định dạng các mục trong menu dropdown */
    .dropdown-menu a {
        color: #333333;
        /* Màu chữ */
    }

    /* Định dạng khi hover (di chuột qua) các mục trong menu dropdown */
    .dropdown-menu a:hover {
        background-color: #f8f9fa;
        /* Màu nền khi hover */
    }

    .delete1 {
        background-color: #dc3545;
        /* Màu nền */
        color: #ffffff;
        /* Màu chữ */
        border: none;
        /* Không viền */
        padding: 0.5rem 1rem;
        /* Kích thước padding */
        border-radius: 0.25rem;
        /* Bo tròn viền */
        text-decoration: none;
        /* Không gạch chân */
        transition: background-color 0.3s ease;
        /* Hiệu ứng chuyển đổi màu nền */
    }

    .update1 {
        background-color: #28a745;
        /* Màu nền */
        color: #ffffff;
        /* Màu chữ */
        border: none;
        /* Không viền */
        padding: 0.5rem 1rem;
        /* Kích thước padding */
        border-radius: 0.25rem;
        /* Bo tròn viền */
        text-decoration: none;
        /* Không gạch chân */
        transition: background-color 0.3s ease;
        /* Hiệu ứng chuyển đổi màu nền */
    }

    li {
        list-style: none;
    }
</style>

<body>
    <header>
        <!-- place navbar here -->
        <nav class="navbar navbar-expand-sm navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="#">Toys Store</a>
                <button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse"
                    data-bs-target="#collapsibleNavId" aria-controls="collapsibleNavId" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="collapsibleNavId">
                    <ul class="navbar-nav me-auto mt-2 mt-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" href="#" aria-current="page">Home <span
                                    class="visually-hidden">(current)</span></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">About</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Contact</a>
                        </li>
                    </ul>
                    <form class="d-flex my-2 my-lg-0">
                        <input class="form-control me-sm-2" type="text" placeholder="Search" />
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                            Search
                        </button>
                    </form>
                </div>
            </div>
        </nav>
    </header>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Product List</h1>
                <ul class="products">
                </ul>
            </div>
        </div>
        <!-- (1): CREATE Task -->
        <!-- -->
        <div class="row justify-content-center mt-5">
            <div class="col-md-6">
                <form action="/quotes" method="POST">
                    <div class="mb-3">
                        <label for="productName" class="form-label">Tên sản phẩm</label>
                        <input type="text" class="form-control" id="productName" placeholder="Tên sản phẩm" name="name"
                            required />
                    </div>
                    <div class="mb-3">
                        <label for="price" class="form-label">Giá</label>
                        <input type="text" class="form-control" id="price" placeholder="Giá" name="quote" required />
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
        </div>
    </div>
    <!-- -->

    <!-- (2) READ Task -->

    <ul class="products">
        <li class="product">
            <table>
                <thead>
                    <tr>
                        <th>NameProduct</th>
                        <th>Price</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i=0; i < quotes.length; i++) { %>
                    <tr>
                        <td><%= quotes[i].name %></td>
                        <td><%= quotes[i].quote %></td>
                        <td> <button class="update1" id="update-button">Update</button>
                            <button class="delete1" id="delete-button">Delete</button></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </li>
    </ul>

    <!-- (3) UPDATE Task -->
  

    <!-- ... -->
    <script src="./main.js"></script>

</body>

</html>